import{callScript,json,type Env,validOrigin,validSession}from'../_shared';const allowed=new Set(['dashboard','listProducts','saveProduct','archiveProduct','deleteProduct','listCategories','saveCategory','listCollections','saveCollection','listSizeCharts','saveSizeChart','listOrders','getOrder','updateOrder','getSettings','saveSettings','listDeliveryRates','saveDeliveryRates']);export const onRequest:PagesFunction<Env>=async({request,env,params})=>{if(!await validSession(request,env.SESSION_SECRET))return json({error:'Session expired'},401);if(!validOrigin(request,env))return json({error:'Invalid request origin'},403);const action=String(params.action);if(!allowed.has(action))return json({error:'Unknown action'},404);try{const payload=request.method==='GET'?Object.fromEntries(new URL(request.url).searchParams):await request.json();return json(await callScript(env,action,payload))}catch{return json({error:'The operation could not be completed'},400)}};
