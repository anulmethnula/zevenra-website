import type {VercelRequest,VercelResponse} from '@vercel/node';
import{appsScriptEnv,body,callScript,json,methodNotAllowed,orderSchema,readCustomerSession,validOrigin}from'./_shared.js';
export default async function handler(req:VercelRequest,res:VercelResponse){if(req.method!=='POST')return methodNotAllowed(res,['POST']);try{const config=appsScriptEnv();if(!validOrigin(req,config))return json(res,{error:'Invalid request origin'},403);const payload=orderSchema.parse(body(req)),identity=readCustomerSession(req),orderPayload=identity?{...payload,email:identity.email,customerId:identity.id}:payload;return json(res,await callScript(config,'createOrder',orderPayload),201)}catch(error){const message=error instanceof Error?error.message:'';if(message.startsWith('CUSTOMER_AUTH_'))return json(res,{error:'Your customer session is invalid. Please sign in again.'},401);return json(res,{error:error instanceof Error&&error.name==='ZodError'?'Please review your order information.':'We could not place the order. Your bag has not been cleared.'},400)}}
