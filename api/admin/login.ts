import type {VercelRequest,VercelResponse} from '@vercel/node';
import{adminLoginSchema,body,env,json,makeSession,methodNotAllowed,sha256,validOrigin}from'../_shared.js';
export default async function handler(req:VercelRequest,res:VercelResponse){if(req.method!=='POST')return methodNotAllowed(res,['POST']);try{const config=env();if(!validOrigin(req,config))return json(res,{error:'Invalid request'},403);const credentials=adminLoginSchema.parse(body(req));const ok=credentials.username===config.ADMIN_USERNAME&&await sha256(credentials.password)===config.ADMIN_PASSWORD_HASH;if(!ok)return json(res,{error:'Invalid credentials'},401);return json(res,{ok:true},200,{'Set-Cookie':`zevenra_session=${makeSession(config.SESSION_SECRET)}; HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=28800`})}catch{return json(res,{error:'Invalid credentials'},401)}}
