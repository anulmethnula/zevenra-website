import {Link} from 'react-router-dom';import type {Product} from '../types';import {money} from '../config/site';
export function ProductCard({product}:{product:Product}){const sold=product.variants.filter(v=>v.active).every(v=>v.stock<1);return <article className="product-card"><Link to={`/product/${product.slug}`}><div className="product-card__media"><img src={product.media[0]?.url} alt={product.media[0]?.alt||product.name} loading="lazy"/><img className="product-card__secondary" src={product.media[1]?.url||product.media[0]?.url} alt="" loading="lazy"/>{sold&&<span>SOLD OUT</span>}</div><div className="product-card__details"><h3>{product.name}</h3><div><p>{money(product.price)}</p>{product.compareAtPrice&&<del>{money(product.compareAtPrice)}</del>}</div></div></Link></article>}
