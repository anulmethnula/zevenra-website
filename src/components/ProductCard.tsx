import {Link} from 'react-router-dom';import {motion} from 'framer-motion';import type {Product} from '../types';import {money} from '../config/site';
export function ProductCard({product}:{product:Product}){const sold=product.variants.filter(v=>v.active).every(v=>v.stock<1),colors=[...new Set(product.variants.filter(v=>v.active).map(v=>v.color))];return <motion.article initial={{opacity:0,y:14}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-30px'}} transition={{duration:.55,ease:[.22,1,.36,1]}}><Link to={`/product/${product.slug}`} className="group block"><div className="relative aspect-[3/4] overflow-hidden bg-stone/15"><img src={product.media[0]?.url} alt={product.media[0]?.alt||product.name} loading="lazy" className="h-full w-full object-cover transition duration-700 ease-out group-hover:scale-[1.018] group-hover:opacity-0"/><img src={product.media[1]?.url||product.media[0]?.url} alt="" loading="lazy" className="absolute inset-0 h-full w-full object-cover opacity-0 transition duration-700 ease-out group-hover:scale-[1.018] group-hover:opacity-100"/>{sold&&<span className="absolute bottom-3 left-3 bg-paper px-2.5 py-1 text-[9px] tracking-[.16em]">SOLD OUT</span>}</div><div className="flex items-start justify-between gap-3 pt-4"><div><h3 className="text-[13px] font-normal tracking-wide">{product.name}</h3>{colors.length>1&&<p className="mt-1.5 text-[10px] tracking-wide text-ink/45">{colors.length} COLOURS</p>}</div><div className="text-right text-[12px]"><p>{money(product.price)}</p>{product.compareAtPrice&&<del className="mt-1 block text-ink/35">{money(product.compareAtPrice)}</del>}</div></div></Link></motion.article>}
