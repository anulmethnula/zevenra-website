import {Link} from 'react-router-dom';import {motion} from 'framer-motion';import type {Product} from '../types';import {money} from '../config/site';
export function ProductCard({product}:{product:Product}){const sold=product.variants.every(v=>v.stock<1);return <motion.article initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-40px'}} transition={{duration:.5}}><Link to={`/product/${product.slug}`} className="group block"><div className="relative aspect-[3/4] overflow-hidden bg-stone/20"><img src={product.media[0].url} alt={product.media[0].alt} loading="lazy" className="h-full w-full object-cover transition duration-700 group-hover:scale-[1.025] group-hover:opacity-0"/><img src={product.media[1]?.url||product.media[0].url} alt="" loading="lazy" className="absolute inset-0 h-full w-full object-cover opacity-0 transition duration-700 group-hover:scale-[1.025] group-hover:opacity-100"/>{product.newArrival&&!sold&&<span className="absolute left-3 top-3 bg-paper px-2.5 py-1 text-[10px] tracking-[.18em]">NEW</span>}{sold&&<span className="absolute inset-x-0 bottom-0 bg-ink/85 py-2 text-center text-[10px] tracking-editorial text-paper">SOLD OUT</span>}</div><div className="flex justify-between gap-3 py-4"><div><h3 className="text-sm font-normal">{product.name}</h3><p className="mt-1 text-xs text-ink/55">{[...new Set(product.variants.map(v=>v.color))].join(' · ')}</p></div><p className="whitespace-nowrap text-sm">{money(product.price)}</p></div></Link></motion.article>}
