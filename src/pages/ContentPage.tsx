import {useLocation} from 'react-router-dom';import {Seo} from '../components/Seo';import {useStore} from '../features/store/StoreContext';
export default function ContentPage(){const {data}=useStore(),key=useLocation().pathname.slice(1);const p=data.pages.find(x=>x.slug===key)||data.pages[0];return <main className="container min-h-[70vh] pb-24 pt-40"><Seo title={p.eyebrow}/><div className="max-w-4xl"><p className="eyebrow text-bronze">{p.eyebrow}</p><h1 className="display mt-5 text-[clamp(3.2rem,8vw,7rem)] leading-[.95]">{p.title}</h1><div className="mt-12 max-w-2xl space-y-5 border-l border-bronze pl-6 text-sm leading-8 text-ink/65">{p.body.map(x=><p key={x}>{x}</p>)}</div></div></main>}
