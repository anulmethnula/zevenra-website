import {useState,type FormEvent} from 'react';import {useNavigate} from 'react-router-dom';
export default function LoginPage(){const nav=useNavigate();const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));try{if(import.meta.env.VITE_DEMO_MODE!=='false'){if(!data.username||!data.password)throw new Error();sessionStorage.setItem('admin-demo','1');nav('/admin');return}const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();nav('/admin')}catch{setError('Invalid credentials. Please try again.')}}return <main className="grid min-h-dvh bg-ink lg:grid-cols-2"><div className="hidden lg:block"><img src="/brand/hero.jpg" alt="" className="h-dvh w-full object-cover opacity-60"/></div><div className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-sm text-paper"><img src="/brand/logo-light.svg" alt="ZEVENRA" className="w-64"/><p className="eyebrow mt-12 text-bronze">Owner access</p><h1 className="display mt-3 text-4xl">Welcome back.</h1><label className="mt-8 block text-xs">Username<input name="username" autoComplete="username" required className="field mt-2 border-white/20 text-white"/></label><label className="mt-4 block text-xs">Password<input name="password" type="password" autoComplete="current-password" required className="field mt-2 border-white/20 text-white"/></label>{error&&<p className="mt-4 text-sm text-red-300">{error}</p>}<button className="btn mt-6 w-full border-paper bg-paper text-ink">Sign in</button><p className="mt-5 text-xs leading-5 text-paper/45">Demo mode accepts any non-empty credentials. Production uses a secure server session and Cloudflare secrets.</p></form></div></main>}
